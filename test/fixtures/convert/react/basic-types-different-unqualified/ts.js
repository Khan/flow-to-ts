import {
  ReactNode,
  ReactText,
  ReactChild,
  ReactPortal,
  ReactNodeArray,
  ReactElement,
} from "react";
let node: ReactNode;
let text: ReactText;
let child: ReactChild;
let portal: ReactPortal;
let nodeArray: ReactNodeArray;
let element: ReactElement;
let children: T | T[];
